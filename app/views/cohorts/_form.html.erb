<%= form_with(model: cohort) do |form| %>
  <% if cohort.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cohort.errors.count, "error") %> prohibited this cohort from being saved:</h2>

      <ul>
        <% cohort.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :d_name, style: "display: block" %>
    <%= form.text_field :d_name %>
  </div>

  <div>
    <%= form.label :d_corhort_id, style: "display: block" %>
    <%= form.text_field :d_corhort_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
